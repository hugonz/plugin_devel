Development of the sql inventory plugin. The idea is to have a plugin to obtain inventory from an SQL DB with a standard table, and then make it extensible by specifying queries for custom DB schemas.

First version will be with SQLite.
